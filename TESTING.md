# Testing

This document records how the system was tested, the datasets used, and the bugs found and fixed during that process. Testing was done manually by running real questions against the system and comparing the output to the expected answer, calculated independently from the raw data.

## Test Datasets

Three deliberately different datasets were used to test the system beyond simple, hand-built sample data:

| Dataset | Rows | Purpose |
|---|---|---|
| `needs.csv` + `distribution.csv` | 36 | Original hand-built sample, standard column names, two-file architecture |
| `ngo_allocation_large.csv` | 1,729 | Standard column names, single combined file, larger scale |
| `ngo_messy_dataset.csv` | 3,046 | Inconsistent column names, inconsistent area name casing/spelling, mixed date formats |
| `ngo_complex_multi_source.csv` | 10,000 | Fully renamed columns, large scale (stress test for schema mapping and report size limits) |

## Test Categories

Each dataset was tested with a mix of:
- **Basic lookups** — "What is the medicine shortage in Multan?"
- **Comparisons** — "Which area has the largest food shortage?" / "Which resource has the smallest gap in Lahore?"
- **Trend questions** — "Is the medicine shortage in Lahore getting worse?"
- **Trend comparisons** — "Which area has the biggest increase/decrease in food shortage over time?"
- **Combined conditions** — "Which area has a worsening food shortage and currently has the largest gap?"
- **Pairwise comparisons** — "Compare Lahore and Multan. Which area has the greater overall shortage?

# 1. Latest Multi-Dataset Testing
## Test Command
```bash
python src/test_newdataset.py
````
This test was performed against three datasets:

1. `ngo_allocation_large.csv`
2. `ngo_messy_dataset.csv`
3. `ngo_complex_multi_source.csv`

---

## 1.1 Standard Dataset

### Dataset
`ngo_allocation_large.csv`
* Type: Standard columns
* Rows: 1,729
### Question
> Which area has the largest food shortage?

### Result

```text
Success: False
Attempts: 3
```

### Validation Failure

```text
Message: Could not produce a reliable data-backed answer after 3 attempts.

Last issue:
The draft states that it is not possible to determine which area has the largest food shortage, but the report actually provides this information in the AREA COMPARISON section, which lists the areas with the largest gaps for food on 2026-12-01.
```

### Interpretation

The dataset and report contain enough information to answer the question, but the Analyst Agent produced an incorrect response claiming that the answer could not be determined.

This indicates a mismatch between:

* The information available in the generated report
* The answer generated by the LLM
* The validation logic

The report already contained an area comparison for food, so the agent should have extracted the largest shortage instead of claiming that the information was unavailable.

### Status

**Failed — Agent reasoning/report utilization issue**

---

# 1.2 Messy Dataset

### Dataset

`ngo_messy_dataset.csv`

* Type: Messy naming
* Rows: 3,046

### Question

> What is the medicine shortage in Multan?

### Result

```text
Success: False
Attempts: 3
```

### Validation Failure

```text
Message: Could not produce a reliable data-backed answer after 3 attempts.

Last issue:
The draft states that the medicine shortage in Multan is most severe on 2026-04-01 with a gap of 62.0 units, and on 2025-09-01 with a gap of 112.0 units. However, the report shows that the gap on 2026-04-01 is 120.0 units, not 62.0 units.
```

### Interpretation

The agent incorrectly selected a value from the available records.

The report contained:

```text
2026-04-01 | needed=186.0 | distributed=66.0 | gap=120.0
2026-04-01 | needed=103.0 | distributed=41.0 | gap=62.0
```

The LLM confused the two records belonging to the same date and reported `62.0` instead of correctly identifying the relevant maximum shortage.

This demonstrates that the system needs stronger deterministic handling when multiple records match the same area/resource/date.

### Status

**Failed — Incorrect record/value selection**

---

# 1.3 Complex Multi-Source Dataset

### Dataset

`ngo_complex_multi_source.csv`

* Type: Fully renamed columns / complex multi-source
* Rows: 10,000

### Question

> What is the medicine shortage in Sialkot?

### Result

```text
Success: False
Attempts: 3
```

### Validation Failure

```text
Message: Could not produce a reliable data-backed answer after 3 attempts.

Last issue:
The deterministic final response failed validation.

This indicates an unexpected report/response mismatch.

Issue:
The draft contains number(s) that do not appear in the report:
[45.0]
```

### Interpretation

The final response contained a numeric value (`45.0`) that could not be found in the generated report.

This is a serious data-grounding problem because the system is expected to generate answers only from verified report data.

The validator correctly rejected the answer instead of allowing an unsupported number to reach the final output.

### Status

**Failed — Unsupported numeric value / grounding mismatch**

---

# 2. Previous Multi-Dataset Testing

## Test Command

```bash
python src/test_newdataset.py
```

---

## 2.1 Standard Dataset

### Question

> Which area has the largest food shortage?

### Result

```text
Success: False
Attempts: 3
```

### Initial Failure

The first failure occurred because the Groq request exceeded the model's token-per-minute limit.

```text
groq.APIStatusError:
Error code: 413

Request too large for model `llama-3.1-8b-instant`

TPM Limit: 6000
Requested: 9689
```

### Interpretation

The entire large dataset/report was being passed into the LLM prompt, resulting in a request larger than the available TPM limit.

This is a system-design issue rather than a dataset calculation issue.

The system should reduce the amount of data sent to the LLM by:

* Performing calculations deterministically with pandas/Python
* Sending only relevant rows/results to the LLM
* Avoiding unnecessary full-dataset information in prompts
* Generating compact reports for the LLM

### Status

**Failed — Prompt/request too large**

---

# 2.2 Standard Dataset — Second Attempt

After reducing/handling the prompt issue, the same test produced:

```text
Success: False
Attempts: 3
```

### Validation Failure

The validator reported:

```text
The draft contains number(s) that do not appear in the report.
```

The reported unsupported numbers included many values such as:

```text
286.0
237.0
49.0
278.0
277.0
281.0
256.0
25.0
228.0
60.0
310.0
300.0
315.0
309.0
298.0
233.0
65.0
292.0
294.0
-2.0
279.0
214.0
...
```

and many additional values.

### Interpretation

The Analyst Agent was exposing large amounts of raw dataset information in the generated answer/report.

The validator then detected numeric values that did not match the final report.

This indicates that the report and answer generation pipeline needs to be more tightly controlled.

The LLM should not independently reproduce large portions of the dataset. Instead:

1. Python should calculate the required result.
2. Python should create a compact evidence section.
3. The LLM should explain only that evidence.
4. The validator should compare the final answer only against the compact report/evidence.

### Status

**Failed — Excessive data exposure and report/answer mismatch**

---

# 3. Previous Successful Multi-Dataset Testing

## 3.1 Messy Dataset

### Dataset

`ngo_messy_dataset.csv`

### Question

> What is the medicine shortage in Multan?

### Result

```text
Success: True
Attempts: 1
```

### Final Answer

The system identified multiple matching allocation records for Multan and Medicine.

Examples included:

```text
Multan | Medicine | 2025-02-01 | needed=698.0 | distributed=418.0 | gap=280.0 | status=Shortage
Multan | Medicine | 2025-07-01 | needed=768.0 | distributed=688.0 | gap=80.0 | status=Shortage
Multan | Medicine | 2025-09-01 | needed=482.0 | distributed=254.0 | gap=228.0 | status=Shortage
Multan | Medicine | 2026-04-01 | needed=186.0 | distributed=66.0 | gap=120.0 | status=Shortage
Multan | Medicine | 2026-12-01 | needed=760.0 | distributed=380.0 | gap=380.0 | status=Shortage
```

The final answer stated that multiple matching records were available and recommended reviewing the allocation records.

### Status

**Passed — Initial test**

---

# 3.2 Complex Multi-Source Dataset

### Dataset

`ngo_complex_multi_source.csv`

### Question

> What is the medicine shortage in Sialkot?

### Result

```text
Success: True
Attempts: 1
```

### Sample Evidence

The system returned multiple Sialkot Medicine records, including:

```text
Sialkot | Medicine | 2024-11-01 | needed=339.0 | distributed=144.0 | gap=195.0 | status=Shortage
Sialkot | Medicine | 2026-02-01 | needed=1164.0 | distributed=885.0 | gap=279.0 | status=Shortage
Sialkot | Medicine | 2026-12-01 | needed=3067.0 | distributed=2480.0 | gap=587.0 | status=Shortage
Sialkot | Medicine | 2025-11-01 | needed=5003.0 | distributed=4014.0 | gap=989.0 | status=Shortage
Sialkot | Medicine | 2026-07-01 | needed=4686.0 | distributed=3327.0 | gap=1359.0 | status=Shortage
Sialkot | Medicine | 2026-05-01 | needed=1890.0 | distributed=0.0 | gap=1890.0 | status=Shortage
Sialkot | Medicine | 2025-08-01 | needed=5158.0 | distributed=0.0 | gap=5158.0 | status=Shortage
Sialkot | Medicine | 2026-03-01 | needed=4565.0 | distributed=2481.0 | gap=2084.0 | status=Shortage
```

The system correctly recognized that multiple matching allocation records existed.

### Status

**Passed — Initial test**

---

# 4. Analyst Agent and Validator Testing

## Question

> What is the medicine shortage in Lahore in July 2026?

### Analyst Agent Result

```text
Detected intent:
{
    'area': 'Lahore',
    'resource': 'Medicine',
    'year': 2026,
    'month': 7,
    'needs_trend': False,
    'needs_comparison': False
}
```

### Relevant Data

```text
area    resource    date        needed_quantity    distributed_quantity    gap    status
Lahore  Medicine    2026-07-01  400                240                    160    Shortage
```

### Draft Answer

```text
According to the report, there is a medicine shortage in Lahore in July 2026,
with a shortage of 160 units.
```

### Generated Report

```text
GAP ANALYSIS SUMMARY
- Records with a shortage: 21
- Records with a surplus: 1
- Records with no gap: 14

RELEVANT GAP DETAILS
- Lahore | Medicine | 2026-07-01 | needed=400 | distributed=240 | gap=160 | status=Shortage
```

---

## Validator Result

```text
Success: True
Attempts: 1
```

### Final Answer

```text
Finding:
Lahore had a medicine shortage of 160 units in July 2026.

Evidence:
400 units were needed, while 240 units were distributed,
leaving a shortage of 160 units.

Recommendation:
NGO staff should review this allocation gap and consider
whether additional medicine resources are needed.
```

### Status

**Passed**

This confirms that the system can successfully:

* Understand a specific date-based question
* Detect area and resource
* Retrieve the relevant row
* Calculate/verify the shortage
* Generate an evidence-backed answer
* Pass the validation stage

---

# 5. Analyst Agent Testing — Why Question

## Question

> Why has Lahore's medicine shortage increased?

### Detected Intent

```text
{
    'area': 'Lahore',
    'resource': 'Medicine',
    'needs_trend': True,
    'needs_comparison': False
}
```

### Draft Answer

```text
Based on the analysis report, it is known that the medicine
shortage in Lahore is worsening. The report specifically states
that the gap has changed from 20 to 160, indicating an increase
in the shortage.

However, the report does not provide a direct reason for the
increase in the medicine shortage in Lahore. Additional
information would be needed to understand the underlying
causes of this worsening trend.
```

### Validator Result

```text
Success: False
Attempts: 3
```

### First Validation Issue

The validator reported that the draft incorrectly included an original gap value.

The report stated:

```text
gap changed by 140 from 20 to 160
```

while the validator interpreted the report wording differently and rejected the draft.

### Subsequent Validation Issue

The validator also identified wording differences involving:

* `20 to 160`
* `changed by 140`
* The statement that additional information is required
* The report summary counts

### Interpretation

The underlying analytical result is correct:

```text
May 2026: gap = 20
June 2026: gap = 90
July 2026: gap = 160
```

The shortage increased by:

```text
160 - 20 = 140
```

However, the natural-language draft and validator were not consistently interpreting the same evidence.

### Status

**Failed — Validator/report wording mismatch**

---

# 6. Tools.py Testing

## Test Command

```bash
python src/tools.py
```

---

# 6.1 Gap Analysis Test

The gap was calculated using:

```text
gap = needed_quantity - distributed_quantity
```

The output correctly classified:

* Positive gap → Shortage
* Negative gap → Surplus
* Zero gap → Okay

### Sample Results

```text
Faisalabad | Clothing | 2026-05-01 | needed=250 | distributed=260 | gap=-10 | Surplus
Faisalabad | Clothing | 2026-06-01 | needed=260 | distributed=260 | gap=0 | Okay

Lahore | Food | 2026-05-01 | needed=800 | distributed=750 | gap=50 | Shortage
Lahore | Food | 2026-07-01 | needed=900 | distributed=770 | gap=130 | Shortage

Lahore | Medicine | 2026-05-01 | needed=300 | distributed=280 | gap=20 | Shortage
Lahore | Medicine | 2026-06-01 | needed=350 | distributed=260 | gap=90 | Shortage
Lahore | Medicine | 2026-07-01 | needed=400 | distributed=240 | gap=160 | Shortage

Multan | Medicine | 2026-05-01 | needed=250 | distributed=150 | gap=100 | Shortage
Multan | Medicine | 2026-06-01 | needed=300 | distributed=140 | gap=160 | Shortage
Multan | Medicine | 2026-07-01 | needed=340 | distributed=130 | gap=210 | Shortage
```

### Result

**Passed**

---

# 6.2 Schema Mapping Test — needs.csv

### Input

`needs.csv`

### Mapping

```text
{
    'area': 'area',
    'resource': 'resource',
    'needed_quantity': 'needed_quantity',
    'date': 'date'
}
```

### Unmapped Columns

```text
[]
```

### Duplicates Removed

```text
0
```

### Missing Values

```text
{}
```

### Result

**Passed**

---

# 6.3 Schema Mapping Test — donations.csv

### Mapping
```text
{
    'resource': 'resource',
    'quantity_received': 'quantity_received',
    'date': 'date'
}
```
### Unmapped Columns
```text
['donor_type']
```
### Missing Values
```text
{}
```
### Interpretation
The relevant donation columns were successfully mapped, while `donor_type` remained unmapped because it was not part of the required schema.
### Result
**Passed**

# 6.4 Messy Dataset Schema Mapping
### Mapping Result
```text
{
    'Reporting_Period': 'date',
    'HH_in_Need': 'needed_quantity'
}
```
### Unmapped Columns
```text
[
    'Beneficiary_Region',
    'Commodity_Type',
    'Qty_Given',
    'Gap_Balance',
    'Programme_Category'
]
```
### Missing Values
```text
{
    'needed_quantity': 1,
    'Gap_Balance': 1
}
```
### Interpretation
The deterministic mapping layer successfully recognized some columns but could not map all messy column names.
This triggered the LLM fallback layer.
### Result
**Partially Passed  LLM fallback required**

# 6.5 LLM Schema-Mapping Fallback Test
### Automatically Mapped Columns
```text
Beneficiary_Region
    → area
    confidence = high
Commodity_Type
    → resource
    confidence = high
Qty_Given
    → distributed_quantity
    confidence = high
```
### Columns Requiring User Confirmation
```text
Gap_Balance
    → none
    confidence = high
    reason = column name suggests an already-calculated value
Programme_Category
    → none
    confidence = high
```

### Interpretation
The LLM fallback successfully identified semantic relationships that deterministic synonym mapping could not identify.
It also correctly avoided automatically treating `Gap_Balance` as an input quantity because it appears to represent an already calculated value.
### Result
**Passed**

# 6.6 Trend Detection Test

### Question
Medicine shortage in Lahore over time.
### Gap History
```text
2026-05-01 → 20
2026-06-01 → 90
2026-07-01 → 160
```
### Result
```text
Medicine shortage in Lahore is worsening
(gap changed by 140 from 20 to 160).
```
### Calculation
```text
160 - 20 = 140
```

### Interpretation
The shortage increased consistently:
```text
20 → 90 → 160
```
Therefore, the trend is worsening.
### Result
**Passed**

# 6.7 Area Comparison Test

### Question
Which area has the largest Medicine gap on 2026-07-01?
### Result
```text
On 2026-07-01, Multan has the largest gap for Medicine
(Shortage, gap=210).
```
### Ranking
```text
1. Multan - gap 210 (Shortage)
2. Lahore - gap 160 (Shortage)
3. Sialkot - gap 10 (Shortage)
4. Faisalabad - gap 5 (Shortage)
```
### Result
**Passed**

# 6.8 Report Generator Test

### Generated Report
GAP ANALYSIS SUMMARY
- Records with a shortage: 21
- Records with a surplus: 1
- Records with no gap: 14
TREND FINDING
- Medicine shortage in Lahore is worsening
  (gap changed by 140 from 20 to 160).
AREA COMPARISON
- On 2026-07-01, Multan has the largest gap for Medicine
  (Shortage, gap=210).
  1. Multan - gap 210 (Shortage)
  2. Lahore - gap 160 (Shortage)
  3. Sialkot - gap 10 (Shortage)
  4. Faisalabad - gap 5 (Shortage)

# 7. Overall Testing Summary

| Test                            | Dataset / Component      | Result                         |
| ------------------------------- | ------------------------ | ------------------------------ |
| Gap Analysis                    | Standard test data       | Passed                         |
| Schema Mapping                  | needs.csv                | Passed                         |
| Schema Mapping                  | donations.csv            | Passed                         |
| Messy Schema Mapping            | Messy dataset            | Partially Passed               |
| LLM Schema Fallback             | Messy dataset            | Passed                         |
| Trend Detection                 | Lahore Medicine          | Passed                         |
| Area Comparison                 | Medicine                 | Passed                         |
| Report Generator                | Combined report          | Passed                         |
| Analyst Agent                   | Specific July 2026 query | Passed                         |
| Validator                       | Specific July 2026 query | Passed                         |
| Why/Trend Question              | Lahore Medicine          | Failed                         |
| Large Dataset LLM Request       | Standard dataset         | Failed — request too large     |
| Large Dataset Answer Validation | Standard dataset         | Failed                         |
| Messy Dataset Query             | Multan Medicine          | Initially Passed, later Failed |
| Complex Dataset Query           | Sialkot Medicine         | Initially Passed, later Failed |
| Multi-Dataset Test              | Standard dataset         | Failed                         |
| Multi-Dataset Test              | Messy dataset            | Failed                         |
| Multi-Dataset Test              | Complex dataset          | Failed                         |

---

# 8. Key Findings from Testing

## What is working

The deterministic data-analysis layer is working correctly for the tested cases.

The following components successfully produced correct results:

* Gap calculation
* Shortage/surplus classification
* Schema mapping for clean datasets
* LLM fallback schema mapping
* Trend detection
* Area comparison
* Report generation
* Specific date-based question answering
* Numeric evidence validation for the successful July 2026 query


## Bugs Found and Fixed

### 1. Token limit exceeded on large datasets
**Symptom:** Questions without a specific area/date returned every matching row to the LLM, exceeding Groq's token-per-minute limit on the 1,729-row dataset.
**Fix:** Capped the number of individual rows shown in any report/answer (50 in the report, 20 in the final answer), with a note when truncated. Summary counts still reflect the full dataset.

### 2. Validator falsely rejecting correct answers
**Symptom:** The semantic (LLM-based) fact-checker occasionally rejected draft answers that were factually correct, sometimes contradicting itself in its own explanation.
**Fix:** Replaced number/date verification with deterministic Python checks (regex-based extraction and comparison) instead of relying on the LLM to judge whether a number appears in the source text. The LLM is now only used for genuinely semantic checks.

### 3. Single combined-file datasets not supported
**Symptom:** The three new test datasets each contain both `needed_quantity` and `distributed_quantity` in one file, but the system was built assuming two separate files.
**Fix:** Added `gap_analysis_from_combined()` and automatic detection of whether one file or two files were supplied, so the same pipeline works for both cases.

### 4. "Which resource has the largest shortage in X?" returned a row dump instead of an answer
**Symptom:** Resource-level comparison questions (comparing resources within one area) were not recognized — only area-level comparison (comparing areas for one resource) existed.
**Fix:** Added a `resource_comparison()` tool, mirroring the existing `area_comparison()` tool, and added deterministic + LLM-based detection of which comparison type a question needs.

### 5. "Smallest" comparisons always returned the largest result
**Symptom:** The system always picked the top of the ranking regardless of whether the question asked for "largest" or "smallest."
**Fix:** Added direction detection (largest/smallest) and selected the correct end of the ranking accordingly.

### 6. Trend questions failed final validation
**Symptom:** Trend answers (e.g. "gap changed from 20 to 160") were rejected because the validator expected a single row's needed/distributed/gap triplet, which trend answers don't have.
**Fix:** Added a dedicated trend-validation path that checks trend numbers against the trend calculation itself, not against a single data row.

### 7. "Biggest decrease" / "decline" answered with an increase
**Symptom:** Trend-comparison questions asking for a decrease returned the area with the biggest increase instead.
**Fix:** Initially patched with a keyword list, which proved too narrow (missed "decline," "drop," "improvement," etc.). Replaced with LLM-based direction detection (the LLM determines whether the question wants increase or decrease), with the keyword list kept only as a fallback if the LLM's response can't be parsed.

### 8. "Largest surplus" answered with a shortage
**Symptom:** Questions asking for a surplus specifically (e.g. "largest surplus for Food") returned the largest shortage instead, because the comparison logic wasn't aware of the shortage/surplus distinction.
**Fix:** Added a `comparison_metric` (shortage / surplus / gap) alongside direction, so the system filters to only surplus rows (or only shortage rows) before selecting a winner. If no matching records exist for the requested metric (e.g. no surplus exists anywhere), the system now explicitly says so instead of silently falling back to a shortage.

### 9. "Smallest gap" mislabeled as "smallest shortage"
**Symptom:** When a question asked generically for "gap" (not specifically shortage or surplus), the answer text still said "shortage" if the winning value happened to be positive.
**Fix:** The final answer now preserves the metric the user actually asked about (gap / shortage / surplus) rather than inferring wording from the sign of the result.

## Known Limitations

- **Area name normalization:** The messy dataset contains area names that differ only in spelling or capitalization (e.g. "Gujranwala" vs "Gujranwla", "Sialkot" vs "Sialkoat"). These are currently treated as distinct areas rather than being merged, since they were not run through the same fuzzy-matching process used for column names. This is a known data-quality limitation, listed in the roadmap.
- **Keyword-based fallbacks:** Where LLM-based detection is used (trend direction, comparison metric), a keyword-list fallback exists for cases where the LLM's response can't be parsed. This fallback is inherently less complete than semantic understanding and may not catch every possible phrasing.
- **Rate limits:** Testing was occasionally interrupted by the Groq API's free-tier rate limits (requests per minute / tokens per day) rather than by application logic errors. These are noted separately in test records and are not counted as application bugs.
